<p id="notice"><%= notice %></p>

<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Listado de Libros</h3>
  </div>

  <div class="panel-body">
    <table class="table">
        <thead>
          <tr>
            <th>Tipo de documento</th>
            <th>Número documento</th>
            <th>String</th>
            <th>Fecha ngreso</th>
            <th>Editorial</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @book_entries.each do |book_entry| %>
            <tr>
              <td><%= book_entry.bookentry_tipo_documento %></td>
              <td><%= book_entry.bookentry_numero_documento %></td>
              <td><%= book_entry.string %></td>
              <td><%= book_entry.bookentry_fecha_ingreso %></td>
              <td><%= book_entry.editorial_nombre %></td>
              <td><%= link_to 'Show', book_entry %></td>
              <td><%= link_to 'Edit', edit_book_entry_path(book_entry) %></td>
              <td><%= link_to 'Destroy', book_entry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>

<br>

<%= link_to 'New Book entry', new_book_entry_path %>
